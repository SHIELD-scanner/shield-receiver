# gRPC Receiver Service Configuration Sample
# Copy this file to .env and update the values as needed

# MongoDB Configuration
MONGO_URI=mongodb+srv://<user>:<password>@<host>/
MONGO_DB=shield

# gRPC Server Configuration
GRPC_PORT=50051
